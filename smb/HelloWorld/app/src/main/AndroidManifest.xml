<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helloworld">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".MyAppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="@string/action_show" />
                <action android:name="@string/action_prev" />
                <action android:name="@string/action_next" />
                <action android:name="@string/action_back" />
                <action android:name="@string/action_play" />
                <action android:name="@string/action_pause" />
                <action android:name="@string/action_stop" />
                <action android:name="@string/action_skip" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/my_app_widget_info" />
        </receiver>
        <receiver
            android:name=".GeoReceiver"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".MediaPlayerService" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/hello" />
        <activity
            android:name=".OptionsActivity"
            android:label="@string/ustawienia"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ListActivity"
            android:label="@string/mojaLista"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SharedListActivity"
            android:label="@string/dzielonaLista"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ShopListActivity"
            android:label="@string/listaSklepow"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>